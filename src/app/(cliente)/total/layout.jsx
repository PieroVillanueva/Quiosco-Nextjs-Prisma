export const metadata = {
  title: "Café - Total y Confirmar Pedido",
  description: "Quiosco Cafetería, confimación del pedido realizado",
};

export default function TotalLayout({ children }) {
  return <>{children}</>;
}
