export const metadata = {
  title: "Café - Resumen",
  description: "Quiosco Cafetería, resumen del pedido realizado",
};

export default function ResumenLayout({ children }) {
  return <>{children}</>;
}
